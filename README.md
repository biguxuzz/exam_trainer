# Тренажёр для подготовки к экзаменам

Веб-приложение для подготовки к экзаменам с отслеживанием прогресса и системой авторизации по Secret строкам.

## Возможности

- 🔐 **Авторизация по Secret строкам** - каждый пользователь имеет индивидуальный доступ (веб-версия)
- 🤖 **Telegram Mini App** - запуск прямо из Telegram без ввода паролей
- 📊 **Отслеживание прогресса** - персональная статистика для каждого пользователя
- 📝 **Режим обучения** - просмотр вопросов с проверкой ответов
- 🧪 **Режим тестирования** - проверка знаний в режиме экзамена
- 📈 **Статистика** - детальная статистика по экзамену и разделам
- 📚 **Поддержка нескольких экзаменов** - переключение между различными экзаменами
- 💾 **Автосохранение сессии** - авторизация сохраняется между сеансами

## Установка

### Требования

- Python 3.9 или выше
- pip

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/exam_trainer.git
cd exam_trainer
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка и запуск

### 1. Создание Secret для пользователей

Перед первым запуском создайте Secret строки для пользователей:

```bash
python generate_secret.py
```

Скрипт создаст:
- Новую Secret строку
- Папку `secrets/{secret}/` с файлом прогресса
- Зарегистрирует Secret в `secrets_config.json`

**Важно:** Сохраните выведенную Secret строку в безопасном месте. Она будет использоваться для входа в приложение.

### 2. Настройка переменных окружения

Для production окружения рекомендуется установить собственный SECRET_KEY:

```bash
# Windows
set SECRET_KEY=ваш-секретный-ключ

# Linux/Mac
export SECRET_KEY=ваш-секретный-ключ
```

**Для Telegram Mini App** также требуется установить токен бота:

```bash
# Windows
set TELEGRAM_BOT_TOKEN=ваш-токен-бота

# Linux/Mac
export TELEGRAM_BOT_TOKEN=ваш-токен-бота
```

Получить токен можно у [@BotFather](https://t.me/BotFather) в Telegram.

Опционально можно настроить максимальный возраст данных авторизации (по умолчанию 24 часа):

```bash
export TELEGRAM_AUTH_MAX_AGE_SECONDS=86400
```

### 3. Запуск приложения

```bash
python trainer_app.py
```

Приложение будет доступно по адресу: **http://localhost:5002**

Для доступа из локальной сети: **http://0.0.0.0:5002**

## Использование

1. Откройте приложение в браузере
2. Введите вашу Secret строку в форму авторизации
3. После успешной авторизации вы получите доступ к:
   - Списку экзаменов
   - Вопросам с фильтрацией
   - Режиму тестирования
   - Персональной статистике прогресса

**Примечание:** Авторизация сохраняется в браузере на 30 дней. При повторном открытии страницы данные загрузятся автоматически.

## Telegram Mini App

Приложение поддерживает запуск как **Telegram Mini App** прямо из Telegram без необходимости ввода Secret.

### Настройка Telegram Mini App

1. **Создайте бота** через [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot` и следуйте инструкциям
   - Сохраните полученный токен бота

2. **Установите переменные окружения**:
   ```bash
   export TELEGRAM_BOT_TOKEN=ваш-токен-бота
   export SECRET_KEY=ваш-секретный-ключ
   ```

3. **Настройте Main Mini App** в @BotFather:
   - Откройте @BotFather
   - Выберите вашего бота
   - Перейдите в `Bot Settings > Configure Mini App > Enable Mini App`
   - Укажите URL вашего приложения: `https://ваш-домен.com/telegram`
   
   **Важно:** URL должен быть HTTPS (кроме тестового окружения Telegram)

4. **Запуск Mini App**:
   - Откройте профиль бота в Telegram
   - Нажмите кнопку **"Launch app"** или используйте прямую ссылку:
     ```
     https://t.me/ваш_бот?startapp
     ```
   - Приложение откроется автоматически, авторизация произойдёт через Telegram

### Особенности Telegram Mini App

- ✅ **Автоматическая авторизация** - не требуется ввод Secret
- ✅ **Прогресс привязан к Telegram аккаунту** - данные сохраняются автоматически
- ✅ **Нативный интерфейс** - использует тему Telegram (светлая/тёмная)
- ✅ **Работает на всех устройствах** - iOS, Android, Desktop, Web

### Хранение данных

- **Веб-версия**: прогресс хранится в `secrets/{secret}/trainer_progress.json`
- **Telegram Mini App**: прогресс хранится в `secrets/tg_{user_id}/trainer_progress.json`

Оба режима работают независимо и не конфликтуют друг с другом.

### Устранение неполадок Telegram Mini App

Если при открытии Mini App возникает ошибка авторизации:

1. **Проверьте переменные окружения:**
   ```bash
   echo $TELEGRAM_BOT_TOKEN  # Должен вывести токен бота
   echo $SECRET_KEY           # Должен вывести секретный ключ
   ```

2. **Проверьте логи сервера:**
   - Ищите сообщения с "Telegram initData validation failed"
   - Проверьте, что `TELEGRAM_BOT_TOKEN` установлен правильно
   - Убедитесь, что токен соответствует боту, для которого настроен Mini App

3. **Проверьте настройки в @BotFather:**
   - URL Mini App должен быть HTTPS (кроме тестового окружения)
   - URL должен указывать на `/telegram` endpoint
   - Пример: `https://ваш-домен.com/telegram`

4. **Проверьте в браузере (консоль разработчика):**
   - Откройте Mini App в Telegram
   - В консоли браузера должны быть логи авторизации
   - Проверьте, что `initData` присутствует и не пустой

5. **Частые проблемы:**
   - ❌ `TELEGRAM_BOT_TOKEN` не установлен → установите переменную окружения
   - ❌ Неверный токен бота → проверьте токен в @BotFather
   - ❌ URL не HTTPS → используйте HTTPS для production
   - ❌ Неверный URL → должен быть `/telegram`, а не `/`

## Запуск в Docker

Контейнер ожидает, что пользовательские данные и (при необходимости) файлы экзаменов лежат в примонтированном томе.

- **обязательное для авторизации**: `secrets_config.json` (список разрешённых Secret)
- **для списка экзаменов/вопросов**: `exam_config.json` и `*_exam_questions.json` (или хотя бы `rp_exam_questions.json`)

Используемые переменные окружения:

- **`SECRETS_DIR`**: путь до папки с данными пользователей (по умолчанию `/app/secrets`)
- **`SECRETS_CONFIG_PATH`**: путь до `secrets_config.json` (рекомендуется `/app/secrets/secrets_config.json`)
- **`EXAM_DATA_DIR`**: папка, где искать `exam_config.json` и файлы с вопросами (например `/app/secrets`)

Пример запуска смотрите в `local-test/docker-compose.yaml`.

## Структура проекта

```
exam_trainer/
├── trainer_app.py              # Основное приложение Flask
├── exam_editor_models.py       # Модели данных для работы с вопросами
├── generate_secret.py           # Скрипт генерации Secret строк
├── requirements.txt            # Зависимости Python
├── .gitignore                  # Исключения для Git
├── README.md                   # Документация
│
├── templates/                  # HTML шаблоны
│   ├── trainer.html           # Основной интерфейс тренажёра
│   └── telegram_trainer.html  # Интерфейс для Telegram Mini App
│
├── sources/                    # Данные экзаменов
│   ├── exam_config.json       # Конфигурация экзаменов
│   └── *_exam_questions.json  # Файлы с вопросами экзаменов
│
├── static/                     # Статические файлы (CSS, JS, изображения)
│
└── secrets/                    # Папка с данными пользователей (не в Git)
    └── {secret}/              # Индивидуальные папки по Secret
        └── trainer_progress.json
```

## Безопасность

⚠️ **Важные замечания:**

- Папка `secrets/` и файл `secrets_config.json` исключены из Git через `.gitignore`
- Не коммитьте Secret строки в репозиторий
- Для production используйте переменную окружения `SECRET_KEY`
- Храните Secret строки пользователей в безопасном месте

## Разработка

### Добавление новых экзаменов

1. Добавьте файл с вопросами в `sources/` (формат: `*_exam_questions.json`)
2. Обновите `sources/exam_config.json` с информацией о новом экзамене

### API Endpoints

**Авторизация:**
- `GET /api/auth/status` - проверка статуса авторизации
- `POST /api/auth/login` - авторизация по Secret (веб-версия)
- `POST /api/auth/telegram` - авторизация через Telegram Mini App initData
- `POST /api/auth/logout` - выход из системы

**Данные:**
- `GET /api/exams` - список доступных экзаменов
- `GET /api/questions` - список вопросов с фильтрацией
- `GET /api/statistics` - статистика по экзамену
- `GET /api/sections` - список разделов экзамена
- `GET /api/question/<question_id>` - получение конкретного вопроса
- `POST /api/question/<question_id>/check` - проверка ответа
- `POST /api/question/<question_id>/mastered` - установка статуса "Усвоен"
- `POST /api/session/start` - начало сессии тестирования
- `POST /api/session/results` - получение результатов сессии

Все API endpoints требуют авторизации (кроме `/api/auth/*`).

## Лицензия

[Укажите вашу лицензию]

## Автор

[Ваше имя/команда]

